services:
  - type: web
    name: nestify
    env: node
    plan: free
    branch: main
    root: .
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
